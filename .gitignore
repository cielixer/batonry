/target
**/*.rs.bk
*.pdb
.DS_Store

# Planning and design notes. Deliberately not tracked: the plan of record lives
# in GitHub issues and the project board, and keeping a second copy in the repo
# means two sources of truth that drift apart. Every CLAUDE.md *is* tracked --
# it is the implementation contract, not a note, and a contract that gates every
# pull request should be reviewable in one.
/docs/

# Local scratch
/_to_delete/
_to_delete/
/.omx/
/crates/winit/target

# Personal Claude Code settings. `.claude/skills/` is tracked; these two are not
# -- they hold per-machine preferences (enabled plugins, output style) and this
# repository is public.
/.claude/settings.json
/.claude/settings.local.json
