[package]
name = "baton-action"
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true

# Does not know `iced` exists. Actions are data.
[dependencies]
baton-core = { path = "../baton-core" }
